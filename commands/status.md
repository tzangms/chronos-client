# Chronos Status

Check your Chronos configuration and connection status.

## Instructions

Run the status script:

```bash
node ${CLAUDE_PLUGIN_ROOT}/scripts/chronos-status.js
```

Show the output to the user. If not configured, suggest running `/chronos:setup`.
